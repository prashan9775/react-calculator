import React from 'react';

const Expression = ({expression}) => {
        return (
            <div className="result">
                <p>{expression}</p>
            </div>
        )
}

export default Expression;

